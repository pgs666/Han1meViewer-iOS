import SwiftUI

struct HomeView: View {
    var body: some View {
        NavigationStack {
            List {
                Section("Latest") {
                    Text("Home repository wiring pending")
                }
            }
            .navigationTitle("Home")
        }
    }
}
