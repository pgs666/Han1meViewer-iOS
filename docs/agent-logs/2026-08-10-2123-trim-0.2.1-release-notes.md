# Trim 0.2.1 Release Notes

## User Input

Original:

```text
删除已下载视频时会同时清理对应的持久化封面，不会留下无用文件；普通缓存清理不会误删下载封面 这一行有必要吗？删了
```

English translation:

```text
Is the line about deleting persistent covers together with downloaded videos and cache cleanup not deleting them necessary? Remove it.
```

## What Changed

- Removed the download-cover cleanup implementation detail from the `v0.2.1` release notes.
- Updated the already-published GitHub Release body to match the repository copy.

## Why

The cleanup behavior is an implementation detail already implied by persistent download-cover support and does not need a separate user-facing changelog entry.

## Verification

- Confirmed the removed sentence no longer appears in the repository release-note file.
- Confirmed the published `v0.2.1` Release body matches the updated file.

## Limits

- No application code or release asset was changed; the existing IPA remains valid.
