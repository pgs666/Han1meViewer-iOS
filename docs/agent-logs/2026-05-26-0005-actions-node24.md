# Agent Log: GitHub Actions Node 24 Runtime

## Summary

- Updated `actions/upload-artifact` from `v5` to `v6`.
- This removes the Node 20 target because `upload-artifact@v6` runs on Node 24 directly.

## Validation

- `26409479524` proved the temporary `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24` switch still leaves a Node 20 target annotation.
- Pending GitHub Actions rerun after switching to `actions/upload-artifact@v6`.
