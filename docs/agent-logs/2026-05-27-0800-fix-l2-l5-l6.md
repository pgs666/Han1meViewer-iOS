# 修复 L2 + L5 + L6

日期：2026-05-27 08:00 CST

## L2 — HomePage.timestamp 字段缺

`HomeFeedSnapshot` 新增 `capturedAtEpochMillis` 字段，从 `HomePage.capturedAtEpochMillis` 传递。
Swift UI 现在可以显示"上次刷新时间"。

## L5 — MySubscriptions.maxPage 命名不统一

将 `MySubscriptions.maxPage` 改为 `page` + `hasNext`，对齐 `UserVideoListPage` 和 `UserPlaylistPage` 的分页模式。

改动文件：
- `HanimeModels.kt`: 模型字段重命名
- `HtmlParser.kt`: 接口新增 `page` 参数
- `KsoupHtmlParser.kt`: 解析器接收 `page`，计算 `hasNext`
- `KtorFollowingRepository.kt`: 传递 `page`
- `FollowingFeature.kt`: 直接使用 `subscriptions.hasNext`

## L6 — baseUrl 注入方式不统一

`SharedAppEnvironment` Kotlin 侧改用 `HanimeNetworkDefaults.DEFAULT_BASE_URL`。
但 KMP `const val` 不能作为 Swift 可见的默认参数值，所以 Swift 侧仍需显式传值。

## 技术教训

KMP `object` 中的 `const val` 不能作为函数默认参数暴露给 Swift。
Swift 看不到默认值，必须显式传参。这是一个已知的 KMP/Native 互操作限制。

## User Input

Original:

```text
review-ios-vs-android-round2.md 看看这个md，然后一条一条修复好有价值的
```

English translation:

```text
Look at this md file, then fix the valuable items one by one.
```
