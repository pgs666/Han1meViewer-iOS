# Sync README To Default Branch

## User Request

中文原文：

> 把readme同步到主分支

English translation:

> Sync the README to the main branch.

## Actions

- Checked the current repository state and confirmed the README update exists on `feature/swiftui-android-video-page`.
- Checked GitHub repository metadata and found that the repository default branch is `feature/ios-kmp-mvp`; no remote `main` branch exists.
- Treated `feature/ios-kmp-mvp` as the project main/default branch for this request.
- Applied the README update from commit `02d1f84` onto `feature/ios-kmp-mvp`.

## Notes

- No code behavior was changed.
- This log was added because the README synchronization modifies project files on the default branch.
