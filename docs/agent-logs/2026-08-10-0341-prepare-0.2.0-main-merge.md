# Prepare 0.2.0 And Merge Feature Branch To Main

## User Input

Original:

```text
把当前分支合并进主分支吧，这版本我准备使用0.2.0版本号
```

English translation:

```text
Merge the current branch into the main branch. I plan to use version 0.2.0 for this release.
```

## What Changed

- Changed the app marketing version in `project.yml` from `1.0` to `0.2.0`.
- Renamed the release-note draft from `v0.1.3-draft.md` to `v0.2.0-draft.md`.
- Updated the draft title, IPA name, tag links, license link, and comparison link for `v0.2.0`.
- Prepared the complete feature branch for integration into `main`.

## Why

The user selected `0.2.0` as the release version. Updating both the release documentation and the app's marketing version ensures the built app and published release use the same version number.

## Mistakes Or Failed Attempts

- The earlier draft inferred `v0.1.3` before the final version number was chosen. It has now been corrected to `v0.2.0`.

## Verification

- Confirmed `origin/main` is the repository's default mainline and is the merge base for this branch.
- Confirmed the configured Git identity is `pgs666 <74764545+pgs666@users.noreply.github.com>`.
- The main-branch GitHub Actions build will verify the merged application code and version metadata.

## Known Limits

- The `v0.2.0` tag and GitHub Release are not created by this merge; they remain a separate release action.
