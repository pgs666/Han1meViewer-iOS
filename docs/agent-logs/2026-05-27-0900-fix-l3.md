# 修复 L3 — WatchHistoryEntity 缺 releaseDate

日期：2026-05-27 09:00 CST

## 改动

1. `WatchHistory.sq`: 新增 `release_date_epoch_millis` 列 (DEFAULT 0)，更新 upsert/insertIfNotExists
2. `HanimeModels.kt`: `WatchHistoryItem` 新增 `releaseDateEpochMillis`
3. `WatchHistoryStore.kt`: `record()` 接受新参数
4. `VideoFeature.kt`: `loadVideo` 中将 `video.uploadTime` (LocalDate) 转为 epoch millis

## 数据来源

Android 使用 `state.info.uploadTimeMillis`，iOS 使用 `video.uploadTime?.atStartOfDayIn(TimeZone.UTC)?.toEpochMilliseconds()`。

## M4 说明

HOME_SECTION_MAPPINGS 硬编码索引问题与 Android 一致（Android 也用 `getOrNull(N)`）。
需要 HTML fixture 才能改进为 CSS 选择器方式，暂时保留现状。

## User Input

Original:

```text
review-ios-vs-android-round2.md 看看这个md，然后一条一条修复好有价值的
```

English translation:

```text
Look at this md file, then fix the valuable items one by one.
```
